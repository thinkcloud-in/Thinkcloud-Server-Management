apiVersion: v1
kind: Service
metadata:
  name: server-management
  namespace: thinkcloud
spec:
  selector:
    app: server-management
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8008
  type: ClusterIP