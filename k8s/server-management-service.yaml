apiVersion: v1
kind: Service
metadata:
  name: server_management
  namespace: thinkcloud
spec:
  selector:
    app: server_management
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8008
  type: ClusterIP