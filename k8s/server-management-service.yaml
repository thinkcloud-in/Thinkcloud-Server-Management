apiVersion: v1
kind: Service
metadata:
  name: server-management
  namespace: thinkcloud
spec:
  selector:
    app: server-management
  ports:
    - protocol: TCP
      port: 8008
      targetPort: 8008
      nodePort: 30080
  type: NodePort